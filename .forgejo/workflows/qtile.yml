name: "qtile"

on: ["push"]

jobs:
  check:
    runs-on: docker
    container:
      image: node:25-bookworm
    steps:
      - uses: actions/checkout@v6
      - name: "Install qtile"
        run: pip install qtile==0.35.0
      - name: "Validate"
        run: qtile check --config ./window-managers/.config/qtile/config.py
