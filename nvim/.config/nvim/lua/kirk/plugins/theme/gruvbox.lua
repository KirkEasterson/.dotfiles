return {
  "ellisonleao/gruvbox.nvim",
  version = "*",
  lazy = false,
  priority = 999,
  opts = {
    italic = {
      operators = true,
    },
  },
  -- config = function(_, opts)
  -- 	require("gruvbox").setup(opts)
  -- 	vim.cmd({
  -- 		cmd = "colorscheme",
  -- 		args = {
  -- 			"gruvbox",
  -- 		},
  -- 	})
  -- end,
}
