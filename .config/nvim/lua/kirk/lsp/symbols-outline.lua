require("symbols-outline").setup({
	-- TODO: investigate is I will actually want line numbers
	-- show_numbers = true,
	-- show_relative_numbers = true,
	symbols = {
		Object = { icon = "", hl = "TSType" },
		Key = { icon = "", hl = "TSType" },
		Event = { icon = "", hl = "TSType" },
	}
})
