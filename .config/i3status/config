general {
        colors = true
        interval = 1
}

#order += "battery all"
order += "tztime local"

battery all {
        format = "%status %percentage %remaining"
}

tztime local {
        format = "%a %d %b %Y | %H:%M:%S"
}
